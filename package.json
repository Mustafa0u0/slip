{
  "name": "slip",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "db": "psql ${DATABASE_URL:-slip} -f db/schema.sql",
    "test": "tsc lib/money.ts --outDir .test-build --module nodenext --target es2022 --moduleResolution nodenext --skipLibCheck && node --test tests/*.test.mjs && rm -rf .test-build"
  },
  "dependencies": {
    "next": "16.3.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.2",
    "tailwindcss": "^4",
    "typescript": "^5",
    "@types/pg": "^8.11.10"
  },
  "description": "Invoices without an account — write one, send a link."
}
